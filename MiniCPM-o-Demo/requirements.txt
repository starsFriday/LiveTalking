
# pip install -r requirements.txt

# Core ML
transformers==4.51.0
accelerate==1.12.0
safetensors>=0.7.0

# MiniCPM-o 4.5
minicpmo-utils[all]>=1.0.5

# Web Service
fastapi>=0.128.0
uvicorn>=0.40.0
httpx>=0.28.0
websockets>=16.0
python-multipart

# Docs rendering
markdown>=3.6
Pygments>=2.18.0

# Audio / Preset (gateway 也直接用:读 preset YAML、加载参考音频)
# 这些过去靠 minicpmo-utils[all] 传递依赖隐式提供,未显式声明;gateway 轻镜像不装
# minicpmo-utils,故在此显式列出。librosa 必须 >=0.10.2 —— 0.9.x 在新 setuptools 下
# 因缺 pkg_resources 导入即崩(见历史故障:preset 加载失败 → sys-prompt 入口消失)。
PyYAML>=6.0
librosa>=0.10.2
soundfile>=0.12.1

# Data / Schema
pydantic>=2.11.0
numpy>=2.2.0

# Utilities
tqdm>=4.67.0

# Dev / Test
pytest>=9.0.0
pytest-asyncio>=1.3.0
